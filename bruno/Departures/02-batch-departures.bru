meta {
  name: Batch Departures
  type: http
  seq: 2
}

post {
  url: {{base_url}}/departures/batch
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
  Content-Type: application/json
}

body:json {
  {
    "stops": ["{{stop_id}}"],
    "limit": 5
  }
}
