meta {
  name: 05 Home Board (Batch Departures)
  type: http
  seq: 6
}

post {
  url: {{base_url}}/departures/batch
  body: json
  auth: none
}

headers {
  x-api-key: {{api_key}}
  Content-Type: application/json
}

body:json {
  {
    "stops": ["{{stop_id}}"],
    "limit": 5
  }
}
