meta {
  name: Login
  type: http
  seq: 3
}

post {
  url: {{base_url}}/auth/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "challengeId": "{{challenge_id}}",
    "signature": "paste-base64url-signature-here"
  }
}
